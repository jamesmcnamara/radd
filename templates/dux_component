{s_flow_header}

import React, {{ Component }} from 'react'
import {{ connect }} from 'react-redux'

// Components

// Modals

// Redux
import mapStateToProps from '../state/select'
import * as {name}Actions from '../state/dux/actions'

// Utils
{s_flow_types_import}
{s_flow_prop_decl}
export default connect(mapStateToProps)
(class {cap_name} extends Component {{
  {s_flow_props_annotation}
  render() {{
    const {{
    }} = this.props

    return (
      <div>
      </div>
    )
  }}
}})

import React, {{ Component }} from 'react'
import {{ connect }} from 'react-redux'

import selectProps from '../redux/select'
import {name}Actions from '../redux/actions/main'
import mergeActions from 'shared/utils/mergeActions'

@connect(selectProps, {name}Actions, mergeActions)
export default class extends Component {{
    render() {{
        return (
            <div />
        )
    }}
}}
